VITE_API_URL=http://localhost:4000/api

# Supabase project -> Settings -> API Keys.
# Use the publishable key (sb_publishable_...), or the legacy anon JWT on
# older projects. Never put the secret key in a browser app.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
